package book.chapter2;

//: initialization/Overloading.java
// Демонстрация перегрузки конструкторов наряду 
// с перегрузкой обычных методов.

import static book.chapter2.Print.*;

/**
 * Класс Tree представляет дерево с высотой.
 */
class Tree {
    /**
     * Поле, хранящее высоту дерева.
     */
    int height;

    /**
     * Конструктор по умолчанию.
     * Инициализирует высоту дерева нулем.
     */
    Tree() {
        print("Caжaeм росток");
        height = 0;
    }

    /**
     * Конструктор с параметром.
     * Инициализирует высоту дерева заданным значением.
     * 
     * @param initialHeight начальная высота дерева
     */
    Tree(int initialHeight) {
        height = initialHeight;
        print("Coданиe нового дерева высотой " + height + " м.");
    }

    /**
     * Выводит информацию о дереве.
     */
    void info() {
        print("Дерево высотой " + height + " м.");
    }

    /**
     * Выводит информацию о дереве с дополнительным сообщением.
     * 
     * @param s дополнительное сообщение
     */
    void info(String s) {
        print(s + ": Дерево высотой " + height + " м.");
    }
}

/**
 * Класс Overloading демонстрирует перегрузку методов.
 */
public class Overloading {
    /**
     * Главный метод программы.
     *
     * @param args аргументы командной строки
     */
    public static void main(String[] args) {
        for (int i = 0; i < 5; i++) {
            Tree t = new Tree(i);
            t.info();
            t.info("Перегружаемый метод");
        }
        // Перегруженный конструктор:
        new Tree();
    }
} /*
   * Output:
   * Создание нового дерева высотой 0 м.
   * Дерево высотой 0 м.
   * Перегруженный метод: Дерево высотой 0 м.
   * Создание нового дерева высотой 1 м.
   * Дерево высотой 1 м.
   * Перегруженный метод: Дерево высотой 1 м.
   * Создание нового дерева высотой 2 м.
   * Дерево высотой 2 м.
   * Перегруженный метод: Дерево высотой 2 м.
   * Создание нового дерева высотой 3 м.
   * Дерево высотой 3 м.
   * Перегруженный метод: Дерево высотой 3 м.
   * Создание нового дерева высотой 4 м.
   * Дерево высотой 4 м.
   * Перегруженный метод: Дерево высотой 4 м.
   * Сажаем росток
   */// :~